from setuptools import setup, find_packages

setup(
    name="deltaverse-api",
    version="0.1.0",
    description="Deltaverse API - Enterprise-level FastAPI application",
    packages=find_packages(),
    python_requires=">=3.11",
)
